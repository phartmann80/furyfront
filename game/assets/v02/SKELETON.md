# Skeleton status — planning only

Rigging remains on hold until the visual gate (grip) passes. This file is a later-work note, not a current task.

Forms are **not** keepable yet. Do not spend time on a production rig until the visual gate passes.

Current GLBs are static meshes (no armature, no skin weights). KF-16 keeps gameplay attachment empties only.

## KF-16 (`ff_wpn_kf16`) — attachment points (do not rename)

| Node | Role |
| --- | --- |
| MuzzleFlash | Muzzle VFX |
| ShellEject | Casing spawn |
| Magazine | Mag mesh / reload |
| AdsAlign | Optic / ADS |

Weapon balance JSON is unchanged.

## Required later (after forms are keepable)

Operators: idle, walk, sprint, crouch, ADS, firing, reload, hit reaction, death.

FPS arms: idle, sway, sprint, ADS, fire/recoil, reload, weapon switching.
