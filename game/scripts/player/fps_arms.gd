class_name FpsArms
extends Node3D
## Viewmodel hands live on the KF-16 GLB (weapon-local). This node stays for pose hooks.

func notify_switch() -> void:
	pass


func pose(_delta: float, _ads: float, _sprinting: bool, _reloading: bool, _speed: float, _punch: Vector3) -> void:
	pass
